/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { SIMPLE_VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const ADD_FUNDS_DISCRIMINATOR = new Uint8Array([
  132, 237, 76, 57, 80, 10, 179, 138,
]);

export function getAddFundsDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(ADD_FUNDS_DISCRIMINATOR);
}

export type AddFundsInstruction<
  TProgram extends string = typeof SIMPLE_VAULT_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountVault extends string | AccountMeta<string> = string,
  TAccountVaultWallet extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountVault extends string
        ? WritableAccount<TAccountVault>
        : TAccountVault,
      TAccountVaultWallet extends string
        ? WritableAccount<TAccountVaultWallet>
        : TAccountVaultWallet,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AddFundsInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type AddFundsInstructionDataArgs = { amount: number | bigint };

export function getAddFundsInstructionDataEncoder(): FixedSizeEncoder<AddFundsInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: ADD_FUNDS_DISCRIMINATOR }),
  );
}

export function getAddFundsInstructionDataDecoder(): FixedSizeDecoder<AddFundsInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getAddFundsInstructionDataCodec(): FixedSizeCodec<
  AddFundsInstructionDataArgs,
  AddFundsInstructionData
> {
  return combineCodec(
    getAddFundsInstructionDataEncoder(),
    getAddFundsInstructionDataDecoder(),
  );
}

export type AddFundsAsyncInput<
  TAccountSigner extends string = string,
  TAccountVault extends string = string,
  TAccountVaultWallet extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  vault?: Address<TAccountVault>;
  vaultWallet?: Address<TAccountVaultWallet>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: AddFundsInstructionDataArgs["amount"];
};

export async function getAddFundsInstructionAsync<
  TAccountSigner extends string,
  TAccountVault extends string,
  TAccountVaultWallet extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SIMPLE_VAULT_PROGRAM_ADDRESS,
>(
  input: AddFundsAsyncInput<
    TAccountSigner,
    TAccountVault,
    TAccountVaultWallet,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AddFundsInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVault,
    TAccountVaultWallet,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIMPLE_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    vaultWallet: { value: input.vaultWallet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vault.value) {
    accounts.vault.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([118, 97, 117, 108, 116])),
        getAddressEncoder().encode(expectAddress(accounts.signer.value)),
      ],
    });
  }
  if (!accounts.vaultWallet.value) {
    accounts.vaultWallet.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([112, 100, 97])),
        getAddressEncoder().encode(expectAddress(accounts.signer.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.vaultWallet),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddFundsInstructionDataEncoder().encode(
      args as AddFundsInstructionDataArgs,
    ),
    programAddress,
  } as AddFundsInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVault,
    TAccountVaultWallet,
    TAccountSystemProgram
  >);
}

export type AddFundsInput<
  TAccountSigner extends string = string,
  TAccountVault extends string = string,
  TAccountVaultWallet extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  vault: Address<TAccountVault>;
  vaultWallet: Address<TAccountVaultWallet>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: AddFundsInstructionDataArgs["amount"];
};

export function getAddFundsInstruction<
  TAccountSigner extends string,
  TAccountVault extends string,
  TAccountVaultWallet extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SIMPLE_VAULT_PROGRAM_ADDRESS,
>(
  input: AddFundsInput<
    TAccountSigner,
    TAccountVault,
    TAccountVaultWallet,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AddFundsInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountVault,
  TAccountVaultWallet,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? SIMPLE_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    vault: { value: input.vault ?? null, isWritable: true },
    vaultWallet: { value: input.vaultWallet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.signer),
      getAccountMeta(accounts.vault),
      getAccountMeta(accounts.vaultWallet),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getAddFundsInstructionDataEncoder().encode(
      args as AddFundsInstructionDataArgs,
    ),
    programAddress,
  } as AddFundsInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountVault,
    TAccountVaultWallet,
    TAccountSystemProgram
  >);
}

export type ParsedAddFundsInstruction<
  TProgram extends string = typeof SIMPLE_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    vault: TAccountMetas[1];
    vaultWallet: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: AddFundsInstructionData;
};

export function parseAddFundsInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAddFundsInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      vault: getNextAccount(),
      vaultWallet: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAddFundsInstructionDataDecoder().decode(instruction.data),
  };
}
