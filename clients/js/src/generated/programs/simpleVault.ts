/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  type Address,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  parseAddFundsInstruction,
  parseInitializeInstruction,
  parseWithdrawFundsInstruction,
  type ParsedAddFundsInstruction,
  type ParsedInitializeInstruction,
  type ParsedWithdrawFundsInstruction,
} from "../instructions";

export const SIMPLE_VAULT_PROGRAM_ADDRESS =
  "AdEUyEPiUi6FmHKP8oPRRjehD8KRUZTppH22QkGWUHCu" as Address<"AdEUyEPiUi6FmHKP8oPRRjehD8KRUZTppH22QkGWUHCu">;

export enum SimpleVaultAccount {
  Vault,
}

export function identifySimpleVaultAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): SimpleVaultAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([211, 8, 232, 43, 2, 152, 117, 119]),
      ),
      0,
    )
  ) {
    return SimpleVaultAccount.Vault;
  }
  throw new Error(
    "The provided account could not be identified as a simpleVault account.",
  );
}

export enum SimpleVaultInstruction {
  AddFunds,
  Initialize,
  WithdrawFunds,
}

export function identifySimpleVaultInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): SimpleVaultInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([132, 237, 76, 57, 80, 10, 179, 138]),
      ),
      0,
    )
  ) {
    return SimpleVaultInstruction.AddFunds;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([175, 175, 109, 31, 13, 152, 155, 237]),
      ),
      0,
    )
  ) {
    return SimpleVaultInstruction.Initialize;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([241, 36, 29, 111, 208, 31, 104, 217]),
      ),
      0,
    )
  ) {
    return SimpleVaultInstruction.WithdrawFunds;
  }
  throw new Error(
    "The provided instruction could not be identified as a simpleVault instruction.",
  );
}

export type ParsedSimpleVaultInstruction<
  TProgram extends string = "AdEUyEPiUi6FmHKP8oPRRjehD8KRUZTppH22QkGWUHCu",
> =
  | ({
      instructionType: SimpleVaultInstruction.AddFunds;
    } & ParsedAddFundsInstruction<TProgram>)
  | ({
      instructionType: SimpleVaultInstruction.Initialize;
    } & ParsedInitializeInstruction<TProgram>)
  | ({
      instructionType: SimpleVaultInstruction.WithdrawFunds;
    } & ParsedWithdrawFundsInstruction<TProgram>);

export function parseSimpleVaultInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedSimpleVaultInstruction<TProgram> {
  const instructionType = identifySimpleVaultInstruction(instruction);
  switch (instructionType) {
    case SimpleVaultInstruction.AddFunds: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: SimpleVaultInstruction.AddFunds,
        ...parseAddFundsInstruction(instruction),
      };
    }
    case SimpleVaultInstruction.Initialize: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: SimpleVaultInstruction.Initialize,
        ...parseInitializeInstruction(instruction),
      };
    }
    case SimpleVaultInstruction.WithdrawFunds: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: SimpleVaultInstruction.WithdrawFunds,
        ...parseWithdrawFundsInstruction(instruction),
      };
    }
    default:
      throw new Error(
        `Unrecognized instruction type: ${instructionType as string}`,
      );
  }
}
